# 烛生 · 领域词汇表

本文件只定义领域语言，不含实现细节。实现决策见 `docs/superpowers/specs/`。

## 仪式夜 (ritual night) — `ritual_date`

一次睡前仪式所归属的**那一晚**，用日期表示。

凌晨 06:00 之前的时刻仍归属**前一晚**。计划 00:30 入睡、00:45 完成的用户，
其仪式夜是前一天，不是完成动作发生的那一天。

- 不是「完成动作的日历日期」
- 一个用户在一个仪式夜最多有一条夜记

### 当前仪式夜 (current ritual night)

此刻所处的仪式夜，等于「当前时刻减 6 小时」所在的日期（用户时区）。

凌晨 02:00 时，当前仪式夜是前一天；上午 09:00 时，是当天。
判断连续按时是否中断，必须以当前仪式夜为基准，不能用日历日期。

## 夜记 (night record)

一次已完成的睡前仪式的记录。**任何完成都产生夜记**，无论是否按时。

包含：仪式夜、计划入睡时刻、实际完成时刻、迟到分钟数、按时资格、
以及**夜记正文**。

- 未完成的夜晚不产生夜记（缺席即断连）
- 一旦产生，完成时刻不可更改；正文可在揭晓窗口开启前修改

### 草稿 (draft)

当前仪式夜尚未提交的输入。**只存在于用户设备本地**，绑定所属仪式夜，
过夜即作废，不上传。

### 夜记正文 (record text)

完成仪式时由草稿固化而来的感恩与计划内容，加密存储。

**可在揭晓窗口开启前（即仪式夜次日 06:00 之前）修改，此后固化。**
修改正文不改变完成时刻，因此不影响按时资格、连续按时与奖励。

## 按时资格 (on-time eligibility) — `is_eligible`

夜记的一个属性。同时满足两个条件才具备资格：

1. **落在资格窗口内** — 完成时刻位于当晚的 `[minTime, maxTime]` 之间（跨午夜）。
   窗口表达「睡前仪式必须在夜间完成」，堵住白天提前刷完流程。
2. **不迟于容差** — 完成时刻不晚于计划入睡时刻加上容差分钟数。

比计划提前入睡**不扣分**，只要仍在资格窗口内。

**只有具备按时资格的夜记才计入连续按时、才可能产生奖励。**
不具备资格的夜记依然是夜记，照常展示。

### 资格窗口 (eligibility window)

当晚允许完成睡前仪式的时间区间，由运营配置的 `minTime`–`maxTime` 界定
（默认 20:00–02:00，跨午夜）。窗口外完成的仪式产生夜记，但不具备按时资格。

## 连续按时 (on-time streak)

截至最近一个仪式夜，**连续且相邻**的、每晚都具备按时资格的夜数。

- 缺席一晚即中断
- 完成但未按时，同样中断
- **中断以当前仪式夜为基准判定**：最近一条夜记的仪式夜若早于「当前仪式夜减一天」，
  连续按时即为 0，不必等到用户下次完成才归零

## 揭晓窗口 (reveal window)

奖励可被揭晓的时间起点：**仪式夜次日 06:00（用户所在时区）**。

窗口开启后，用户下一次打开小程序时揭晓。

- 不是「日历日期变了」
- 不是「完成后满 24 小时」
- 语义是「睡过一觉，第二天早上醒来看到」
- 06:00 的边界与仪式夜的归属边界一致

## 抽卡 (draw)

揭晓时从**艺术作品池**中随机取出作品的动作。允许重复抽中同一幅。

抽卡次数在揭晓那一刻定格，此后不因连续按时天数变化而改变。

**抽卡次数规则**（按当时的连续按时天数）：

- 基础 1 抽；连续按时满 **14 晚**后基础升为 **2 抽**（永久，直至中断）
- 里程碑额外 +1 抽：第 3、7、14、30 晚，此后每满 30 晚一次

规则的设计意图是让「连续」优于「中断重来」，且尽早成立。

门槛之所以是 14 而非 30：定在 30 时，前 37 晚故意每 3 晚断一次仍能拿到更多抽卡
（第 30 晚：刷法 40 抽 vs 坚持 35 抽），交叉点远在第 38 晚。降到 14 后
交叉点提前至**第 16 晚**，30 晚累计 51 抽对刷法 40 抽。

交叉点由 `backend/tests/test_e2e_flow.py::test_correction_4_incentive_curve` 钉死，
规则若再变会立刻失败。

## 收藏 (collection)

用户已获得的全部奖励。区分两个计数：

- **累计卡片数** — 抽中的总次数，含重复
- **不同作品数** — 去重后的作品数量

## 艺术作品池 (art pool)

可被抽中的作品集合。作品必须字段完整、已上架、且未被撤回，才在池中。

### 作品的三种状态

| 状态 | 进抽卡池 | 已收藏用户可见 | 用途 |
|---|---|---|---|
| **上架** | 是 | 是 | 正常 |
| **下架** | 否 | **是** | 暂时不再发放，已获得的是用户资产 |
| **撤回** | 否 | **否** | 版权等法务原因必须停止展示 |

被任何用户收藏过的作品**不可物理删除**（`rewards.art_id` 外键 `ON DELETE RESTRICT`），
只能下架或撤回。

**所以「撤回」不是「下架」的重复，而是「删除」在删不掉时的替代品**：

- 想让它不再被抽到、但已获得的人保留 → **下架**
- 想让它对所有人消失（版权、法务） → **撤回**
- 想彻底删掉这条记录（填错了、图错了，且还没人收藏过） → **删除**

后台的作品库页面有一个可展开的说明把这三条写在界面上 —— 光看按钮名字
（下架 / 撤回 / 删除）分不清它们的后果，那是必须解释的。

## 历史固化 (historical immutability)

已发生的判定不因日后的规则或配置变更而改变。

按时资格、迟到分钟数、抽卡次数在**发生当时**就写入夜记并固定，
不是每次查询时重算。运营调整容差或资格窗口，只影响此后的仪式夜。

用户变更时区同理：已有夜记的仪式夜不重算。

**例外**：作品的元数据与文章是**活引用**，修改后已收藏用户同步可见
（错别字修正应当全局生效）。奖励只记录作品标识，不快照作品内容。

## 注销 (account deletion)

用户主动删除账号。**物理删除**该用户的全部数据：夜记（含加密正文）、
奖励、设置、事件。不做软删除，不保留残影。
